<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Confirmation</title>
    <style>
        body {
            font-family: 'Roboto', Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f9fafc;
            color: #333;
            line-height: 1.6;
        }

        .order-container {
            max-width: 900px;
            margin: 50px auto;
            background-color: #ffffff;
            border-radius: 12px;
            box-shadow: 0px 10px 20px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }

        .order-header {
            background-color: #00796b;
            color: #ffffff;
            padding: 20px;
            text-align: center;
        }

        .order-header h2 {
            margin: 0;
            font-size: 24px;
        }

        .order-header p {
            margin: 10px 0 0;
            font-size: 16px;
        }

        .order-details {
            padding: 20px;
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
        }

        .product-info, .order-summary, .order-status {
            flex: 1 1 45%;
            padding: 15px;
            background-color: #f9f9f9;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .product-info img {
            max-width: 100%;
            border-radius: 8px;
            margin-bottom: 15px;
        }

        .product-info h3 {
            margin: 10px 0 5px;
            font-size: 20px;
        }

        .product-info p {
            margin: 5px 0;
            font-size: 16px;
        }

        .order-summary h3 {
            margin-bottom: 15px;
        }

        .order-summary p {
            font-size: 16px;
            margin: 5px 0;
        }

        .order-status {
            background-color: #f1f8f6;
            border: 1px solid #e0f2f1;
            padding: 15px;
            text-align: center;
        }

        .status-item {
            margin: 10px 0;
            padding: 10px;
            background-color: #dcedc8;
            border-radius: 5px;
            font-weight: bold;
            font-size: 16px;
            color: #33691e;
        }

        .order-actions {
            text-align: center;
            padding: 20px;
        }

        .order-actions button {
            padding: 10px 30px;
            border: none;
            background-color: #00796b;
            color: white;
            border-radius: 25px;
            font-size: 16px;
            cursor: pointer;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
        }

        .order-actions button:hover {
            background-color: #004d40;
            box-shadow: 0px 6px 12px rgba(0, 0, 0, 0.15);
        }

        .order-actions button:hover {
            background-color: #004d40;
            box-shadow: 0px 6px 12px rgba(0, 0, 0, 0.15);
        }
    </style>
</head>
<body>
    <div class="order-container">
        <div class="order-header">
            <h2>Order ID: <span><%= order.orderId %></span></h2>
            <p>Placed On: <span><%= new Date(order.createdAt).toLocaleDateString() %></span></p>
            
        </div>

        <div class="order-details">
            <div class="product-info">
                <h3>Products</h3>
                <% for (const item of order.items) { %>
                    <% if (item.productId) { %>
                        <img src="<%= item.productId.images && item.productId.images[0] ? item.productId.images[0] : '/api/placeholder/150/150' %>" alt="Product Image">
                        <h3><%= item.productId.name %></h3>
                        <p>Quantity: <%= item.quantity %></p>
                      
                    <% } else { %>
                        <p>Product details are not available for this item.</p>
                    <% } %>
                <% } %>
            </div>
            <div class="order-actions">
                <button onclick="window.location.href='/order-tracking'">Track Order</button>
            </div>
         
        </div>
    </div>
</body>
</html>



